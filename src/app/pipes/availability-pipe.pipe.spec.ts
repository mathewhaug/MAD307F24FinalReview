import { AvailabilityPipePipe } from './availability-pipe.pipe';

describe('AvailabilityPipePipe', () => {
  it('create an instance', () => {
    const pipe = new AvailabilityPipePipe();
    expect(pipe).toBeTruthy();
  });
});
