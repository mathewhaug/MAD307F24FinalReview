import { SitterStatusDirective } from './sitter-status.directive';

describe('SitterStatusDirective', () => {
  it('should create an instance', () => {
    const directive = new SitterStatusDirective();
    expect(directive).toBeTruthy();
  });
});
